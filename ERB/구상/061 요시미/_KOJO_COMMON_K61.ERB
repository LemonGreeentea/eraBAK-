;===============================================
; EraBlueArchiveK 요시미 구상 공통 구상
; 제작자 : 레몬녹차
; 최초 작성일 : 2024. 4. 22.
; 최종 수정일 : 2024. 5. 5.
;  
; 리액션 혜자 츤데레 요시미의 구상입니다. >>>\(>ㅁ<)/<<<
; 
;-------------------------------------------------
;=================================================
;●구상의 존재 판정
;=================================================
@KOJO_EXIST_K61

;=================================================
;●개시시
;※상황에 관계없이 항상 실행된다. 취급 주의※
;=================================================
@KOJO_TRAIN_START_K61

;커맨드 시작 시 처녀 여부 확인
SIF TALENT:처녀
	CFLAG:230 = 1

;커맨드 시작 시 애널처녀 여부 확인
SIF TALENT:애널처녀
	CFLAG:231 = 1

;커맨드 시작 시 키스미경험 여부 확인
SIF TALENT:키스미경험
	CFLAG:232 = 1

;커맨드 시작 시 동정 여부 확인
SIF TALENT:동정
	CFLAG:232 = 1

;=================================================
;●종료시
;※상황에 관계없이 항상 실행된다. 취급 주의※
;=================================================
@KOJO_TRAIN_END_K61

;=================================================
;●커맨드 실행시(이 캐릭터가 플레이어측 때)
;※상황에 관계없이 항상 실행된다. 취급 주의※
;=================================================
@KOJO_COM_TARGET_K61

;=================================================
;●커맨드 실행시(이 캐릭터가 플레이어측 때)
;※상황에 관계없이 항상 실행된다. 취급 주의※
;=================================================
@KOJO_COM_PLAYER_K61

;=================================================
;●커맨드 실행 후(파라미터의 변동 처리가 끝나고 나서 불려 간다)
;※상황에 관계없이 항상 실행된다. 취급 주의※
;=================================================
@KOJO_COM_AFTER_K61

;커맨드 시작 시 처녀 여부 확인
SIF !TALENT:처녀
	CFLAG:230 = 0

;커맨드 시작 시 애널처녀 여부 확인
SIF !TALENT:애널처녀
	CFLAG:231 = 0

;커맨드 시작 시 키스미경험 여부 확인
SIF !TALENT:키스미경험
	CFLAG:232 = 0

;커맨드 시작 시 동정 여부 확인
SIF !TALENT:동정
	CFLAG:232 = 0

;[공허]상태의 경우, 입이 막히고 있는 경우는 구상을 표시하지 않는다
IF TALENT:공허 || IS_EQUIP_CONTINUE(TARGET, "입장착")
	RETURN 0
ENDIF
SETCOLOR 0xFDD48E

;------------------------------------------------
;호칭의 설정
;------------------------------------------------
;노예면 '주인님'이라고 부름
IF IS_SLAVE(TARGET)
	LOCALS:0 = 주인님
;아나타가 [선생]이면 '선생님'이라고 부름
ELSEIF TALENT:MASTER:선생 == 1
	LOCALS:0 = 선생님
;그 외엔 이름으로 부름
ELSE
	LOCALS:0 = %ANAME(MASTER)%
ENDIF

;-------------------------------------------------
;절정 구상
;-------------------------------------------------
;-------------------------------------------------
;Ｃ절정 구상
;-------------------------------------------------
IF NOWEX:Ｃ절정 > 0
	;첫 C절정
	IF CFLAG:220 == 0
		CFLAG:220 = 1
		PRINTFORML 「읏, 뭐야... 이거어...」
		PRINTFORMW 요시미는 처음 느껴보는 절정에 몸부림치고 있다.
		PRINTFORML 「그마안, 제발... 멈춰줘...!」
		PRINTFORMW 「크으읏... 햐아앙... 시러어...」
	;그 이후 절정
	ELSE
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW 「힛, 시러엇... 가버려엇...」
			CASE 1
				PRINTFORMW 「햐아앙... 안대에, 간다앗...」
		ENDSELECT

	ENDIF
ENDIF

;-------------------------------------------------
;Ｖ절정 대사
;-------------------------------------------------
IF NOWEX:Ｖ절정 > 0
	;첫 V절정
	IF CFLAG:221 == 0
		CFLAG:221 = 1
		PRINTFORMW 「뭐야... 안쪽이 이상해...!」
		PRINTFORML 「그만, 멈춰줘 더 이상 하면.... 힛」
		PRINTFORMW 요시미는 몸을 떨면서 절정에 달했다.
		PRINTFORMW 「크응읏...! 흐읏... 아냐, 안갔거든?!」
	;그 이후 V절정
	ELSE
		SELECTCASE RAND:2
			CASE 0
				PRINTFORML 요시미는 몸을 떨며 절정했다
				PRINTFORML 「으읏.... 시럿, 히잇...!」
			CASE 1
				PRINTFORML 「안댓... 또 간다...♡」
				PRINTFORMW 요시미의 질이 꾹 조이면서 절정에 달했다.
		ENDSELECT
	ENDIF
ENDIF

;-------------------------------------------------
;처음Ａ절정
;-------------------------------------------------
IF NOWEX:Ａ절정 > 0
	;처음 A절정
	IF CFLAG:222 == 0
		CFLAG:222 = 1
		PRINTFORMW 「흣, 흐으... 힛, 안돼, 엉덩이인데 가앗...♡」
		PRINTFORML 요시미의 장내가 꾸욱 조이면서 생전 처음 느껴보는 절정에 달하였다.
		PRINTFORMW 「흐... 후... 말도 안돼... 」
	;그 이후 A절정
	ELSE
		PRINTFORML 요시미는 고개를 젖히고 엉덩이로 절정에 달하였다.
		PRINTFORMW 「읏... 엉덩이로 가앗...♡」
	ENDIF

ENDIF

;-------------------------------------------------
;처음Ｂ절정
;-------------------------------------------------
IF NOWEX:Ｂ절정 > 0
	;처음Ｂ절정
	IF CFLAG:223 == 0
		CFLAG:223 = 1
		PRINTFORMW 「뭐야... 가슴이... 힛! 히야앗」
		PRINTFORMW 요시미는 처음 느껴보는 절정에 눈물을 흘리며 몸을 떨고 있다.
	;그 이후Ｂ절정
	ELSE
		PRINTFORML 「햐앗... 가슴 기분죠앗...」
		PRINTFORMW 「햐읏....♡」
	ENDIF
ENDIF

;-------------------------------------------------
;처음Ｍ절정
;-------------------------------------------------
IF NOWEX:Ｍ절정 > 0
	IF CFLAG:224 == 0
		CFLAG:224 = 1
		;PRINTFORMW 
	ENDIF
ENDIF

RESETCOLOR

;=================================================
;●단체 엔딩 관계
;=================================================
;
;
;
;
;
;
;
;
;
;
;★여기로부터 먼저는 엔딩이 기술되고 있습니다★
;네타바레를 피하고 싶은 (분)편은 열람하지 않게 주의해 주세요.
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
@KOJO_SINGLE_ENDING_K61
SETCOLOR 0xFDD48E
;단체 엔딩
;전도시를 통일했을 때, 호감도 5000이상 또한 전캐릭터중 가장 호감도가 높다고 표시된다
;PRINTFORMW 

;RETURN의 값을 0→1으로 바꾸면, Default의 단체 엔딩 메세지가 스킵 된다
RESETCOLOR
RETURN 0

@KOJO_DEAD_ENDING_K61
SETCOLOR 0xFDD48E
;참수 엔딩
;자세력이 멸망 했을 때의 선택지로 「……베어라」를 선택해 주인공이 사망했을 경우에, 호감도 5000이상 또한 전캐릭터중 가장 호감도가 높다고 표시된다
;PRINTFORMW 

;RETURN의 값을 0→1으로 바꾸면, Default의 단체 엔딩 메세지가 스킵 된다
RESETCOLOR
RETURN 0
